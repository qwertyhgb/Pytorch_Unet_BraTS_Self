# ==================== 深度学习框架 ====================
# PyTorch 核心库（根据你的 CUDA 版本选择合适的版本）
torch>=2.0.0
torchvision>=0.15.0

# ==================== 数据处理 ====================
# NumPy：数值计算和数组操作
numpy>=1.24.0

# OpenCV：图像读取、保存和处理
opencv-python>=4.8.0

# Pillow：图像处理（备用）
Pillow>=10.0.0

# ==================== 可视化 ====================
# Matplotlib：绘图和可视化
matplotlib>=3.7.0

# ==================== 进度条 ====================
# tqdm：训练和推理进度条显示
tqdm>=4.65.0

# ==================== 数据增强（可选）====================
# Albumentations：强大的图像增强库（医学图像分割推荐）
# albumentations>=1.3.0

# ==================== 医学图像处理（可选）====================
# NiBabel：读取 NIfTI 格式的医学图像
# nibabel>=5.1.0

# SimpleITK：医学图像处理工具包
# SimpleITK>=2.3.0

# ==================== 其他工具 ====================
# scikit-learn：评估指标计算（可选）
# scikit-learn>=1.3.0

# pandas：数据分析和统计（可选）
# pandas>=2.0.0

# ==================== 开发工具（可选）====================
# Jupyter：交互式开发
# jupyter>=1.0.0
# ipykernel>=6.25.0

# TensorBoard：训练可视化（可选）
# tensorboard>=2.14.0

# ==================== 说明 ====================
# 安装方法：
# 1. 基础安装（必需）：
#    pip install -r requirements.txt
#
# 2. 如果需要 GPU 支持，请先安装对应 CUDA 版本的 PyTorch：
#    # CUDA 11.8
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    
#    # CUDA 12.1
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 3. 可选库（取消注释后安装）：
#    - albumentations: 数据增强
#    - nibabel/SimpleITK: 医学图像格式支持
#    - tensorboard: 训练可视化
#
# 4. 验证安装：
#    python -c "import torch; print(f'PyTorch: {torch.__version__}'); print(f'CUDA available: {torch.cuda.is_available()}')"
